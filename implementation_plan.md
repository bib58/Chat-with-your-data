The application will allow users to upload datasets or connect databases, ask natural language questions, and receive detailed answers, data tables, and dynamically generated charts, powered by a LangGraph Agent.


- Start FastAPI backend (`uvicorn main:app --reload`).
- Start Vite frontend (`npm run dev`).

